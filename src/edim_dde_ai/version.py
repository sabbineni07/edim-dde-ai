"""Package version.

Business purpose:
  Single source for the installed package version string (CLI ``version``,
  packaging metadata consumers).

Public API:
  - ``__version__`` — semver string
"""

__version__ = "1.0.0"
