# Runtime dependencies (keep in sync with pyproject.toml [project.dependencies])
langgraph>=0.2
langchain-core>=0.3
PyYAML>=6

# Optional postgres extras (StateStore / RecommendationStore / EDIM_CHECKPOINTER=postgres)
# Keep in sync with pyproject.toml [project.optional-dependencies].postgres
psycopg[binary]>=3.1
psycopg-pool>=3.2
langgraph-checkpoint-postgres>=2.0
